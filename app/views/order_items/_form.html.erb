<%= form_for(@order_item) do |f| %>
  <% if @order_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_item.errors.count, "error") %> prohibited this order_item from being saved:</h2>

      <ul>
      <% @order_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div><h2><%= @order_item.product.title %></h2></div>
  <div><h5>Stock Left: <%= print_stock(@order_item.product.stock) %></h5></div>
  <br />

  <div>Cart quantity: <%= @order_item.quantity %></div>
  <br />

  <div class="field">
    <%= f.label :quantity, 'Update your cart quantity:' %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
